# ─────────────────────────────────────────────────────────────
#  _config_demo.yml  —  DEMO / STAGING (axiomorphe.ca/cgbvrr)
#  Override file. Contains ONLY the keys that differ from
#  production. Everything else is inherited from _config.yml.
#  Build with:  jekyll build --config _config.yml,_config_demo.yml
# ─────────────────────────────────────────────────────────────

url: "https://axiomorphe.ca"
baseurl: "/cgbvrr"               # site lives in a subdirectory

# Keep the RSS feed valid under the subdirectory too
feed:
  path: feed.xml                 # resolves to /cgbvrr/feed.xml via baseurl
